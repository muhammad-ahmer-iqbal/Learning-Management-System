@model IEnumerable<LMS.coordinator>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<style>
    .Coordinator, .Coordinator i {
        background-color: #EDEDED !important;
        color: #F36A5A !important;
    }

        .Coordinator i {
            color: #F36A5A !important;
        }
</style>

<div class="row">
    <div class="col-md-12">
        <!-- Advanced Tables -->
        <div class="panel panel-default">
            <div class="panel-heading">
                <b>Coordinator Details</b>
                <p>
                    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-block btn-md btn-info" })
                </p>
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.coord_id)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.coord_name)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.coord_email)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.coord_address)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.coord_contact)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.coord_salary)
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr class="odd gradeX">
                                    <td>
                                        @Html.DisplayFor(modelItem => item.coord_id)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.coord_name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.coord_email)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.coord_address)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.coord_contact)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.coord_salary)
                                    </td>
                                    <td class="text-center">
                                        @Html.ActionLink("Edit", "Edit", new { id = item.coord_id }, new { @class = "btn btn-primary btn-sm" }) |
                                        <button class="btn btn-danger btn-sm" onclick="del(this, '@Html.DisplayFor(modelItem => item.coord_id)')">Delete</button>
                                        @using (Html.BeginForm())
                                        {
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" value=@Html.DisplayFor(modelItem => item.coord_id) name="delete" />
                                            <input type="submit" value="Confirm Delete" class="btn btn-danger btn-sm" id=@Html.DisplayFor(modelItem => item.coord_id) style="display: none;" />
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <!--End Advanced Tables -->
    </div>
</div>